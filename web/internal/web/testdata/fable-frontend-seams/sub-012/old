        Three demo lines are embedded byte-exact from <span class="mono">.agentfactory/store/formulas/</span>.
        Connect the real folder (Chromium browsers) and every card below becomes the actual file —
        open, edit, and pour straight back into your working tree.